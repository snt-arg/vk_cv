rosrust::rosmsg_include!(
    std_msgs / Bool,
    sensor_msgs / CompressedImage,
    sensor_msgs / Image,
    sensor_msgs / CameraInfo,
    geometry_msgs / Point,
    geometry_msgs / PoseStamped,
);
